const testConfig = require('../config/config');

module.exports = () => {
  describe('Add user', () => {

    it('should be possible when not existing', () => {
      console.log('Execute code for testing to add user');
    });

    it('Should not be possible when username exists', () => {
      console.log('Execute code for testing to add user');   
    })
  });
}