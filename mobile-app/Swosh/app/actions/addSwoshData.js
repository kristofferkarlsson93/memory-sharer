import { addSwoshDataActions as actions } from './actionTypes';

export const addSwoshData = (image) => {

}

const addGreeting = (greeting) => ({ type: actions.ADD_GREETING });

const addContacts = () => ({ type: actions.ADD_CONTACTS })